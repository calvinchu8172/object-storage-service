DeliveriesTable:
  Type: AWS::DynamoDB::Table
  Properties:
    TableName: ${self:service}-${self:provider.stage}-deliveries
    AttributeDefinitions:
      - AttributeName: delivery_id
        AttributeType: S
    KeySchema:
      - AttributeName: delivery_id
        KeyType: HASH
    ProvisionedThroughput:
      ReadCapacityUnits: 5
      WriteCapacityUnits: 5
